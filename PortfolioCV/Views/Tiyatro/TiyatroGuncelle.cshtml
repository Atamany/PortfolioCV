@model Tiyatro
@{
    ViewData["Title"] = "TiyatroGuncelle";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="film-form-wrapper">
    <form asp-action="TiyatroGuncelle" method="post" class="film-form" autocomplete="off">
        <h2 class="film-form-title">Tiyatro Güncelle</h2>
        @if (!ViewData.ModelState.IsValid)
        {
            <div class="film-form-error">Lütfen tüm alanları eksiksiz ve doğru doldurun.</div>
        }
        <input type="hidden" asp-for="TiyatroId" class="film-form-input" />
        <div class="film-form-group">
            <label for="Ad">Tiyatro Adı</label>
            <input type="text" asp-for="Ad" class="film-form-input" value="@Model?.Ad" required />
        </div>
        <div class="film-form-group">
            <label for="TiyatroUrl">Tiyatro Url</label>
            <input type="url" asp-for="TiyatroUrl" class="film-form-input" value="@Model?.TiyatroUrl" required />
        </div>
        <div class="film-form-group">
            <label for="ResimUrl">Resim Url</label>
            <input type="url" asp-for="ResimUrl" class="film-form-input" value="@Model?.ResimUrl" required />
        </div>
        <button type="submit" class="film-form-btn">Kaydet</button>
    </form>
</div>




