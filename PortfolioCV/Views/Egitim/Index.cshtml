@model List<Egitim>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="film-list-wrapper">
    <div class="film-list-header">
        <div class="film-list-header-row">
            <h2 class="film-list-title">Eğitim Listesi</h2>
            <a href="/Egitim/EgitimEkle" class="film-add-btn"><i class="fas fa-plus"></i> Eğitim Ekle</a>
        </div>
    </div>
    <div class="film-table-responsive">
        <table class="film-table">
            <thead>
                <tr>
                    <th>Eğitim (Yıl)</th>
                    <th>Güncelle</th>
                    <th>Sil</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.OkulAdi - @item.Bolum (@item.Yil)</td>
                        <td><a href="/Egitim/EgitimGuncelle/@item.EgitimId" class="film-action-btn update"><i class="fas fa-edit"></i></a></td>
                        <td><a href="/Egitim/EgitimSil/@item.EgitimId" class="film-action-btn delete"><i class="fas fa-trash-alt"></i></a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />



